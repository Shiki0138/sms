# sms (salon-management-system)用tmux設定

# 自動保存設定（5分ごと）
set -g @continuum-save-interval '5'
set -g @continuum-restore 'on'

# ステータスバーにセッション名を表示
set -g status-left '[#S] '
set -g status-left-length 20

# ウィンドウ名を自動更新
set -g automatic-rename on

# 履歴保持数を増やす
set -g history-limit 10000